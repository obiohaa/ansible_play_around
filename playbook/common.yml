---
- name: play ansible
  hosts: dev
  remote_user: ubuntu
  become: yes
  become_user: root
  tasks:
    - name: Update apt repo
      apt:
        update_cache: yes

    - name: install Python
      apt:
        name: python3
        state: present

    - name: install httpd
      package:
        name: httpd
        state: present

    - name: run httpd
      service:
        name: httpd
        state: started
        enabled: yes

    - name: create content
      copy:
        content: "Congratulations on installing ansible"
        dest: /var/www/html/index.html
