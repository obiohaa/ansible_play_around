pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo 'application build...'
            }
        }

        stage("test") {
            steps {
                echo 'application test...'
            }
        }

        stage("deploy") {
            steps {
                echo 'application deploy...'
            }
        }
    }
}